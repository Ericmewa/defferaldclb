# SMTP configuration (Gmail example)
# For Gmail use: SMTP_HOST=smtp.gmail.com, SMTP_PORT=465, SMTP_SECURE=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=ericmewa5@gmail.com
SMTP_PASS=jbzq ksec dtry qldr
SMTP_SECURE=true
EMAIL_FROM="Deferrals <noreply@deferral.com>"

# Frontend URL used in links
# FRONTEND_URL=http://localhost:5173

FRONTEND_URL=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,
http://localhost:5177,http://localhost:5178,http://localhost:5179,http://localhost:5180


# Optional fallback CO email if deferral doesn't include one
CO_EMAIL=co@example.com
